type Query {
    students: [Student]
    student(id: Int!): Student
    findStudentsByFirstName(firstName: String!): [Student]
}

type Student {
    id: Int!
    firstName: String!
    lastName: String!
    registration: String!
    papers: [Paper]
}

type Paper {
    id: Int!
    title: String!
}